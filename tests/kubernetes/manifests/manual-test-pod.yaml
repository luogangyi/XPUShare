apiVersion: v1
kind: Pod
metadata:
  name: manual-dynamic-test
  labels:
    app: xpushare-manual-test
spec:
  restartPolicy: Never
  containers:
  - name: test
    # Same image as script
    image: registry.cn-hangzhou.aliyuncs.com/lgytest1/xpushare:pytorch-add-small-5fed3e5b
    # Same command as script (except sleep duration)
    # command: ["python3", "-c", "import torch; import time; a=torch.randn(2048, 2048).cuda(); print('Starting loop');\nwhile True:\n  torch.matmul(a, a); time.sleep(0.01)"]
    env:
    - name: XPUSHARE_DEBUG
      value: "1"
    resources:
      limits:
        xpushare.com/gpu: 1
